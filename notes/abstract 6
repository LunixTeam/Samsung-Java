XML (eXtensible Markup Language)
  Расширяемый язык разметки для хранения и передачи данных.
  Данные имеют структурированный вид, понятный как компьютерам, так и человеку.

Манифест (Manifest)
  Файл (чаще всего в формате XML), который содержит метаданные.
  Описывает состав, структуру и свойства группы файлов или всего приложения.

Верстка
  Процесс создания пользовательского интерфейса (UI).
  Заключается в преобразовании дизайн-макета в рабочий код.

Цикл for (цикл с параметром)
  Синтаксис: for (инициализация; условие; шаг) { тело_цикла }
  Используется, когда известно точное количество повторений.

Массив в Java
  Структура данных для хранения набора элементов одного типа.
  Элементы располагаются в непрерывном блоке памяти.
  Бывают одномерные и многомерные массивы.
  Нумерация элементов (индексация) всегда начинается с 0.
